generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("mysql://root:dhrhdtka887!!@127.0.0.1:3306/phonebook")
}

model myprofile {
  id       Int    @id @default(autoincrement())
  img      String
  username String
}

model friends {
  id       Int    @id @default(autoincrement())
  img      String
  username String
  birth    String
  phone    String
}
